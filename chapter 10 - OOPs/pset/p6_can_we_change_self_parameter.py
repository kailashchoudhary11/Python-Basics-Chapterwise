class Demo:
    # def __init__(self, name): # We can change the parameter self to any other name as well
    #     self.name = name

    def __init__ (this, name):
        this.name = name

sample = Demo("kai")
print(sample.name)